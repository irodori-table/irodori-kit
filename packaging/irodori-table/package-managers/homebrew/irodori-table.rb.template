cask "irodori-table" do
  version "__VERSION__"
  sha256 "__SHA256_DMG__"

  url "https://github.com/irodori-table/irodori-table/releases/download/v#{version}/__MACOS_DMG_ASSET__",
      verified: "github.com/irodori-table/irodori-table/"
  name "Irodori Table"
  desc "Open-source SQL workbench for multiple database engines"
  homepage "https://irodori-table.github.io/irodori-docs/"

  livecheck do
    url :url
    strategy :github_latest
  end

  app "Irodori Table.app"

  # Quit the running app before an upgrade replaces it so the previous version
  # is fully torn down first.
  uninstall quit: "dev.irodori.table"

  zap trash: [
    "~/Library/Application Support/dev.irodori.table",
    "~/Library/Caches/dev.irodori.table",
    "~/Library/Preferences/dev.irodori.table.plist",
  ]
end
