[package]
name = "wasm-sql-dialect"
version = "0.1.0"
edition = "2021"
license = "0BSD"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
